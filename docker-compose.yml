version: '3'
services:
  frontend:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./
    ports:
      - '80:80'
    volumes:
      - /app/node_modules
      - .:/app
    stdin_open: true
