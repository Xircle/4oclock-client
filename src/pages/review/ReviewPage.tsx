import styled from "styled-components";
import ReviewCard from "../../components/review/ReviewCard";
import ReviewCardContainer from "../../components/review/ReviewCardContainer";

interface Props {}

// const places: Place[] = [];
// places.map((place) => {
//   <ReviewThumbnail key={place.id} placeName={place.name} review={reviews} />;
// });


// #열렸던 이팅모임 후기
export default function ReviewPage(props: Props) {

  return (
    <div>
      <h1>#열렸던 이팅모임 후기</h1>
      {/* <ReviewCardContainer key={place.id} places={places} /> */}
    </div>
  );
}
